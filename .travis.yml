language: node_js
node_js:
  - "lts/*"
install:
  - npm install
script:
  - hexo clean
  - hexo g
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN  # Set in travis-ci.org dashboard, marked secure
  keep-history: true
  local-dir: ./public/
  on:
    branch: master
